# GitHub Tutorial

This is a simple README.md file. This file usually contains information that the users of this repository should know.

```Blackjack.html``` <br>
A simple blackjack game that is playable on your web browser. User data does not persist once you close the site. Use username `Alex` and password `e` to start with $100,000.

```Clock.jar``` <br>
A simple Java program that displays your current time. Might require Java to run.

```TicTac.jar``` <br>
A 4x4 TicTacToe game made with Java. Might also require Java to run.
